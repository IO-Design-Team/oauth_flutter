name: oauth_flutter
description: "A drop-in OAuth2 solution for Flutter apps. Handles auth, token storage, and token refresh."
version: 0.0.1
homepage:

environment:
  sdk: ^3.0.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.6.0
  flutter_secure_storage: ^9.2.2
  flutter_web_auth_2: ^4.0.0-alpha.8
  fresh_dio: ^0.4.2

dev_dependencies:
  rexios_lints: ^8.0.0
